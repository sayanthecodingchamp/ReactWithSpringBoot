server:
  port: 8090
spring:
  datasource:
    url: jdbc:postgresql://localhost:8080/jwt
    username: postgres
    password: 29102002
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
